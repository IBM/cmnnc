- create a `CONV -> CONV` onnx graph
   - execute it using onnxruntime
   - execute it on the simulator

- create a

```
CONV ---> CONV ---> CONV  ---> ADD
      |                    ^
      |                    |
      +--------------------+
```

onnx graph
   - execute it using onnxruntime
   - execute it on the simulator
